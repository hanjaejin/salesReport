# CLAUDE.md — 30초 매장 브리핑 데모 (MIN) 구현 지침

> 이 파일은 Claude Code 세션의 작업 규칙이다. **기능 명세는 이 폴더의
> `30초매장브리핑_바이브코딩_구현설계서_v1.3.1.md`가 유일한 원천**이며,
> 이 파일과 명세가 충돌하면 명세가 이긴다. 명세에 없는 결정이 필요하면
> 임의 구현하지 말고 사용자에게 질문한다.

## 프로젝트 한 줄 요약

역 편의점 점포장에게 매일 아침 3줄 브리핑을 배달하고 일일 보고서를 자동 생성하는
서비스의 데모. 합성 데이터(점포 3곳 × 13개월) → SQLite → 집계 → 브리핑 →
Streamlit 화면 → xlsx 보고서까지 전 과정이 실제로 흐른다.
발표 5분 라이브 데모용 — **하루 안에 완성이 목표.**

## 절대 불변식 (Invariants — 모든 코드 변경 전 자문할 것)

1. **숫자는 코드만 만든다** — 문장 계층(briefing 템플릿)은 계산 JSON 값의 치환만.
   어떤 산술 연산도 금지 (반올림 포함 — 반올림은 JSON 저장 시점에 완료).
2. **FACT_RECEIPT_ITEM × FACT_PAYMENT 직접 조인으로 금액 합산 금지** —
   grain이 달라 매출이 부풀려진다. 집계는 FACT별로 따로.
3. **멱등** — `load_period()`를 같은 인자로 재실행해도 결과 동일
   (날짜 단위 DELETE→INSERT).
4. **파생 시드** — 난수는 반드시 `hash((20260723, DEPT_CD, SALEDATE))` 기반
   독립 Generator. 전역 순차 난수 금지 (부분 재생성 결정성의 전제).
5. **실컬럼명 동결** — 명세 4장 DDL의 테이블·컬럼명을 한 글자도 바꾸지 않는다.
6. **개인정보 컬럼 미생성** — 카드번호·회원번호류는 스키마에도 없다.
7. **서버측 생성 원칙** — 브리핑 문장은 배치에서 생성·DB 저장 완료.
   화면(Streamlit)은 저장된 것을 표시만 한다. 화면 계층에서 재계산·LLM 호출·
   API 키 요구·외부 네트워크 호출 금지.

## 코딩 표준 (사내 규칙 — korail-coding-rule)

- **TDD**: Red → Green → Refactor. 테스트 없는 기능 코드 작성 금지.
  각 마일스톤은 명세 10장의 해당 테스트를 먼저 작성한다.
- **Tidy First**: 구조 변경(리네임·이동·정리)과 행위 변경(기능)을 같은 커밋에 섞지 않는다.
- 모든 함수: 타입 힌트 + Google 스타일 docstring (한국어 설명 허용).
- `print` 금지 — `src/common/logger.py`의 공통 logger 사용.
- pandas는 벡터화 — `iterrows` 금지. SQL은 `SELECT *` 금지(컬럼 명시).
- SQLAlchemy Core 사용, SQLite 방언 종속 문법 금지 (추후 PostgreSQL 교체 대비).
- 커밋 메시지: 한국어, `[M2] 합성 데이터 생성기: 점포별 시간대 프로파일 구현` 형식.
  구조 변경 커밋은 `[tidy]` 접두.

## 작업 방식 — 마일스톤 게이트

명세 11장의 M1→M2→M3→M4→M5 순서로만 진행한다.

- 각 마일스톤 시작: 해당 명세 절을 다시 읽고, 작업 계획(파일·테스트 목록)을
  3~5줄로 보고 후 착수.
- 각 마일스톤 완료: ① 전체 `pytest` 통과 ② 실행 검증 명령과 결과 요약 제시
  ③ **사용자 확인을 받은 뒤에만 다음 마일스톤 진행.**
- 막히면 30분 규칙: 같은 문제로 30분 이상 소모 시 우회안 2개와 함께 사용자에게 보고.

### 마일스톤 요약 (상세는 명세 11장·13장)

| M | 산출물 | 검증 |
|---|---|---|
| M1 | 골격 + DDL(schema.py) + Extractor 계약(base.py) + oracle_stub | `pytest tests/test_schema.py` |
| M2 | synth.py + sample.py (점포 3곳·13개월·프로파일 차등·동일자 취소 1.5%) | 정합 테스트 3종 |
| M3 | pipeline.py (`--from/--to`, 멱등) | `test_load_idempotent`, `test_partial_regen_deterministic` |
| M4 | aggregate.py + briefing.py (임계값 G4=5.0/G2=25.0, 템플릿·로테이션) | 브리핑 일치·임계 경계·침묵일 테스트 |
| M5 | app/main.py(관리자 재생성 버튼 포함) + report/daily_report.py | 명세 12장 데모 체크리스트 전항목 |

## 검증 명령 (완료 보고 시 이 명령들의 결과를 첨부)

```bash
python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
pytest -q                                            # 전체 테스트
python -m src.load.pipeline --from 20250701 --to 20260731   # 전체 구축
python -m src.load.pipeline --from 20260701 --to 20260722   # 멱등 재실행 시연
streamlit run src/app/main.py                        # 데모 화면
```

## 하지 말 것 (바이브코딩 사고 방지 — 명세 14장)

- 명세에 없는 기능 추가 금지 (개선 아이디어는 코드가 아니라 보고로).
- 브리핑 템플릿 문자열 임의 수정 금지 — 명세 7.4의 문자열·변형 목록이 전부다.
- 외부 CDN·API·모델 다운로드 금지. 데모용 하드코딩 화면 금지(모든 값 DB 경유).
- 화면에 전문용어 노출 금지 (AI·LLM·분석·객단가 → "1인당 구매액").
- 에러를 삼키는 빈 except 금지. 임시 파일·주석 처리 코드 커밋 금지.

## 완료의 정의 (Definition of Done)

명세 12장 데모 리허설 체크리스트 전항목 통과 + `pytest` 전체 통과 +
README에 "두 명령 실행법"(구축·화면)과 클라우드 배포 절차(명세 15장) 정리 + data/pos_mockup.db 동봉 + 세 점포의 브리핑이 서로 다름
(L=아침 G2 / M=점심 G2 / S=침묵일 존재)을 캡처로 증빙.

---

## 세션 시작 프롬프트 (터미널에서 첫 입력으로 붙여넣기)

```
이 폴더의 CLAUDE.md와 30초매장브리핑_바이브코딩_구현설계서_v1.3.1.md를 읽고
30초 매장 브리핑 데모 구현을 시작합니다.

먼저: ① 명세를 읽었다는 확인으로 절대 불변식 7개를 한 줄씩 요약하고
② M1 작업 계획(생성할 파일·테스트 목록)을 보고해 주세요.
제가 계획을 승인하면 M1을 TDD로 진행합니다.
마일스톤마다 검증 결과를 보여주고 제 확인 후 다음으로 넘어갑니다.
```
