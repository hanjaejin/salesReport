# 부록 B. 여러 매장 관리자 화면

> 본 명세 `30초매장브리핑_바이브코딩_구현설계서_v1.3.1.md` 의 확장이다.
> 본문과 충돌하면 **본문이 이긴다.** 이 부록은 본문 9장(화면)이 규정한
> "점포장 1인용 1페이지"에 **보는 사람 한 종류를 더한다.**
>
> 관련: [ADR-0013](adr/0013-관리자-화면을-그룹-브리핑으로-만든다.md)

## B.1 왜 넣는가

지금 화면은 점포장에게 "오늘 당신 매장에서 무엇을 하라"를 말한다.
매장 셋을 맡은 사람에게는 그 질문이 다르다 — **"오늘 어느 매장부터 볼 것인가."**

현재 구조로 그 답을 얻으려면 사이드바 selectbox를 세 번 바꿔야 하고,
바꾸는 사이에 앞 매장 숫자를 기억해야 한다. 30초 안에 끝나지 않는다.

세 매장의 판단 재료는 **이미 다 만들어져 있다.** `BRIEFING_DAILY` 에
매장별로 매출·손님·1인당·카드·위험재고가 들어 있다. 새로 계산할 것이 아니라
**한 화면에 모으는 문제**다.

## B.2 절대 조건 — 화면은 여전히 계산하지 않는다

가장 쉬운 구현은 화면이 매장 셋을 읽어 `sum()` 하는 것이다. **이것을 금지한다.**

불변식 1은 "숫자는 코드만 만든다", 불변식 7은 "서버측 생성 — 화면은 표시만"이다.
세 매장 매출을 화면이 더하면 **그 합계는 화면이 만든 숫자**가 된다.
합계가 하나 허용되면 다음은 평균, 그다음은 증감률이다. 경계는 처음에 긋는다.

그래서 그룹 요약도 **배치가 만들어 저장하고, 화면은 JSON 1건을 읽어 표시만 한다.**
매장 화면이 3초 안에 뜨는 이유와 같은 이유로, 관리자 화면도 빠르다.

## B.3 스키마 — `BRIEFING_DAILY_GROUP` 추가

본문 4장 DDL에 테이블 **1개**를 더한다. 기존 테이블은 한 글자도 바꾸지 않는다.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `SALEDATE` | TEXT | `YYYYMMDD` (PK) |
| `PAYLOAD_JSON` | TEXT | 그룹 요약 JSON (B.5) |

점포 단위가 아니므로 `DEPT_CD` 가 없다. 날짜 하나에 행 하나다.

**`BRIEFING_DAILY` 에 `DEPT_CD='ALL'` 로 끼워 넣지 않는다** — `DIM_STORE` 에 없는
점포코드를 만드는 셈이라 참조 관계가 무너지고, 매장 목록 질의마다 예외가 생긴다.

## B.4 생성 규칙 (본문 7장 확장)

**매장별 브리핑이 전부 만들어진 뒤에** 그 날짜의 브리핑들을 읽어 만든다.
원장이나 마트를 다시 집계하지 않는다 — 이미 확정된 값을 모으기만 한다.

```
build_briefings(...)          # 기존: 매장별 브리핑 저장
  └─ build_group_briefing(...)  # 추가: 그 날짜 브리핑들을 모아 1건 저장
```

멱등은 기존과 같다 — 날짜 단위 DELETE→INSERT (불변식 3).

### 정렬

매장 행은 **매출 내림차순 고정**이다. 매일 순서가 바뀌면 관리자가
"둘째 줄이 어느 매장이었지"를 매번 다시 읽어야 한다.

대신 **먼저 볼 매장**을 따로 하나 뽑아 위에 적는다. 우선순위는 본문 7.3의
카드 우선순위를 그대로 쓴다.

| 순위 | 조건 | 뜻 |
|---|---|---|
| 1 | `G3` 발동 (재고 부족) | 오늘 채워야 한다 |
| 2 | `G2` 발동 (시간대 쏠림) | 진열 시점이 있다 |
| 3 | 없음 | 조용한 날 |

같은 순위가 여럿이면 **매출이 큰 매장**이 앞선다. 아무 매장도 1·2위가 아니면
"먼저 볼 매장"은 비운다 — 침묵할 줄 아는 것이 이 제품의 신뢰 조건이다 (본문 7.3).

## B.5 계산 JSON

```json
{
  "schema_version": 1,
  "saledate": "20260609",
  "store_count": 3,
  "total_sale_amt": 4409450,
  "total_deal_cnt": 1353,
  "group_avg_ticket": 3259,
  "attention_dept_cd": "901001",
  "attention_count": 1,
  "stores": [
    {
      "dept_cd": "901001",
      "dept_nm": "중앙역 대형점",
      "sale_amt": 2999800,
      "deal_cnt": 929,
      "avg_ticket": 3229,
      "dow_diff_pct": 33.4,
      "dow_baseline_available": true,
      "status": "STOCK",
      "status_text": "재고 7개 부족",
      "risk_count": 7
    }
  ]
}
```

- `group_avg_ticket` 은 **총매출 ÷ 총손님** 을 마트 계층에서 반올림해 넣는다.
  매장별 1인당 구매액의 평균이 아니다 (손님 수가 다르므로 값이 달라진다).
- `total_deal_cnt` 가 0이면 `group_avg_ticket` 은 0이다 (0 나눗셈 금지 — 본문 7.2와 동일).
- `status` 는 `STOCK` · `PEAK` · `CALM` 셋뿐이다. 매장 브리핑의 2줄을 차지한 카드와 **항상 일치**한다.
- `attention_dept_cd` 는 없으면 `null`.

## B.6 문장 (본문 7.4 확장)

관리자 화면에 새 **브리핑 문장**은 만들지 않는다. 매장 문장은 그 매장 것이고,
여기서는 **상태 라벨**만 쓴다. 라벨 목록이 전부이며 임의 변형을 금지한다.

| status | 라벨 | 색 |
|---|---|---|
| `STOCK` | `재고 {risk_count}개 부족` | 🔴 |
| `PEAK` | `{peak_name}에 몰림` | 🟡 |
| `CALM` | `조용한 날` | ⚪ |

먼저 볼 매장 한 줄:

```
오늘은 {dept_nm}부터 확인해 보세요
```

아무 매장도 해당 없으면:

```
오늘은 세 매장 모두 평소대로예요
```

### 지켜야 할 금지 (본문 14장 그대로)

- 발주 수량·재고 수치 상세 금지 (부록 A.5와 동일).
- 전문용어 금지 — `분석`·`예측`·`객단가`·`증감률`·`결품`·`리드타임` 을 쓰지 않는다.
- 매장 간 우열을 단정하는 문장 금지. "A매장이 부진합니다" 같은 평가는 만들지 않는다.
  숫자를 나란히 보여 주고 판단은 사람이 한다.
- 원인 단정 금지 — "비 때문에" 같은 문장은 데이터에 근거가 없다.

## B.7 화면 (본문 9장 확장)

사이드바 맨 위에 **보기 전환** 하나를 둔다.

```
◉ 점포장 화면      ← 기본값 (기존 화면 그대로)
○ 여러 매장 보기
```

멀티페이지(`pages/`)를 쓰지 않는다 — 발표 중 페이지 이동은 흐름이 끊기고,
URL이 바뀌어 되돌아오기 어렵다.

**여러 매장 보기** 구성 (위에서부터):

1. 헤더 — `{날짜} · {store_count}개 매장` + 목업 배지 (상시)
2. 먼저 볼 매장 한 줄 (B.6)
3. 매장 표 — 매장명 · 어제 매출 · 손님 · 1인당 · 상태 라벨
4. 합계 줄 — 총매출 · 총손님 · 1인당
5. 하단 고지 (본문 9장과 동일)

매장 행을 누르면 그 매장의 점포장 화면으로 넘어간다 — 관리자가
"어느 매장부터"를 정한 다음 바로 그 매장을 볼 수 있어야 한다.

기존 점포장 화면은 **한 픽셀도 바꾸지 않는다.**

## B.8 테스트 (본문 10장 확장)

| 테스트 | 확인 |
|---|---|
| `test_group_totals_match_store_sum` | 그룹 합계 = 매장별 값의 합 (DB에서 각각 읽어 비교) |
| `test_group_avg_ticket_is_total_based` | 1인당 = 총매출÷총손님 (매장 평균이 아님) |
| `test_group_avg_ticket_zero_deals` | 손님 0명이면 0 (0 나눗셈 없음) |
| `test_group_status_matches_store_cards` | `status` 가 매장 브리핑의 2줄 카드와 일치 |
| `test_group_store_order_is_by_sale_amt` | 매출 내림차순 고정 |
| `test_attention_follows_card_priority` | G3 > G2 > 없음, 동순위는 매출 큰 쪽 |
| `test_attention_is_null_when_all_calm` | 모두 조용하면 먼저 볼 매장 없음 |
| `test_group_briefing_is_idempotent` | 같은 날짜 재실행 시 동일 |
| `test_admin_view_makes_no_arithmetic` | 관리자 화면 함수에 산술 연산 없음 (AST 정적 검사) |
| `test_admin_view_shows_no_jargon` | 금지 용어 0개 |
| `test_admin_view_renders_without_exception` | AppTest 로 실제 렌더링 |

`test_admin_view_makes_no_arithmetic` 이 이 부록의 **핵심 방어선**이다.
화면에 `sum()` 이나 `+` 가 들어오는 순간 깨진다.

## B.9 배포

`BRIEFING_DAILY_GROUP` 은 화면이 읽으므로 **읽기 모델**이다 —
`publish.READ_MODEL_TABLES` 에 넣는다 (ADR-0011). 396행이라 발행 시간에 영향이 없다.

## B.10 보고서 확장 (개정 — 2026-08-28)

소유자 요청으로 화면의 성격이 바뀐다. **매장 목록**이 아니라
**중간 관리자가 팀장에게 올리는 보고서**다. 보는 사람이 한 번 더 위로 올라간다.

### 무엇이 늘어나는가

| 블록 | 내용 | 숫자를 만드는 곳 |
|---|---|---|
| 총평 | 기준일 합계 · 지난주 같은 기간 대비 | 로더(SQL) |
| 매장별 요약 | 매출 · 손님 · 1인당 · **비중** · 평소 요일 대비 · 상태 | 배치(JSON) |
| 매출 흐름 | 최근 14일 매장별 꺾은선 | 로더(SQL) |
| 시간대 비교 | 세 매장 시간대별 매출 | 로더(SQL) |
| 많이 팔린 상품 | 매장별 TOP3 | 로더(SQL) |
| 기간 합계 | 최근 7일 · 14일 매장별 누적 | 로더(SQL) |
| 내려받기 | 그룹 보고서 xlsx | 보고서 계층 |

### 기간 집계는 어디서 하는가 — 경계를 다시 긋는다

B.2는 "합계도 배치가 만든다"를 규정했다. 이 규정은 **기준일 하루**에 대한 것이다.
기간 집계(최근 7·14일)는 다르게 다룬다.

| 종류 | 만드는 곳 | 근거 |
|---|---|---|
| **기준일 요약** (합계·비중·상태·먼저 볼 매장) | 배치 → `BRIEFING_DAILY_GROUP` | 상태와 먼저 볼 매장은 **카드 판정**에 의존한다. 매일 고정된 값이라 저장이 맞다 |
| **기간 집계** (최근 7·14일, 지난주 대비) | 로더의 SQL | 기간은 화면에서 고르는 축이라 저장 대상이 아니다. 날짜마다 저장하면 같은 숫자가 396번 중복된다 |

**두 경우 모두 화면은 산술을 하지 않는다.** 배치가 만들거나 DB가 집계한다.
화면 함수 안의 `sum()`·`+` 는 여전히 금지이고 정적 검사가 막는다.
로더는 `load_totals()` 가 이미 쓰던 방식이다 (명세 9장 관리자 비교).

### 문장을 만들지 않는다

총평을 "지난주보다 8.3% 늘었어요" 같은 **문장**으로 쓰지 않는다.
문장을 만들면 화면이 문장 계층이 되어 불변식 7이 흔들린다.
값과 라벨로만 보여 준다 — `지난주 같은 기간 대비  +8.3%`.

### 계산 JSON 추가 필드 (B.5 확장)

매장 행에 `share_pct` (그 날 전체 매출에서 차지하는 비중, 소수 1자리) 하나를 더한다.
반올림은 배치에서 끝낸다.

## B.11 그룹 보고서 xlsx (본문 9장 확장)

팀장에게 올리려면 **건네줄 파일**이 있어야 한다. 매장별 일일 보고서(명세 9장)의
그룹판이며, 같은 방식으로 메모리에서 만들어 내려받는다 (쓰기 권한 불필요).

| 시트 | 내용 |
|---|---|
| 요약 | 날짜 · 매장 수 · 합계 · 1인당 · 먼저 볼 매장 |
| 매장별 | 매출 · 손님 · 1인당 · 비중 · 평소 요일 대비 · 상태 |
| 기간 | 최근 7일 · 14일 매장별 누적 |
| 시간대 | 매장 × 시간대 매출 |

파일명은 `여러매장_보고_YYYYMMDD.xlsx` 로 한다.
전문용어 금지와 목업 배지 고지는 매장 보고서와 동일하게 적용한다.

## B.12 테스트 추가

| 테스트 | 확인 |
|---|---|
| `test_group_share_pct_sums_to_hundred` | 매장 비중 합계가 100%에 수렴 |
| `test_group_share_pct_zero_sales` | 매출 0원이면 비중 0 (0 나눗셈 없음) |
| `test_period_summary_matches_mart` | 기간 집계 = 마트 SUM |
| `test_period_summary_handles_missing_prior` | 직전 기간이 없으면 대비를 비운다 |
| `test_group_report_has_expected_sheets` | xlsx 시트 4종 |
| `test_group_report_numbers_match_screen` | 보고서 숫자 = 화면 숫자 |
| `test_group_report_shows_no_jargon` | 전문용어 0개 |

## B.13 규모 대응 — 매장 1,300개 (개정 2 — 2026-08-28)

소유자 확인: **실제 환경은 매장 1,300개**다. 샘플이 3개라서 성립하던 것들이 깨진다.

### 무엇이 깨지는가 (실측)

| 항목 | 매장 3개 | 매장 1,300개 | 판정 |
|---|---|---|---|
| 그룹 JSON | 957 B | 0.3 MB/일 · 0.1 GB/396일 | 크기는 견디나 **화면에 다 못 쓴다** |
| 매장별 행 나열 | 3행 | 1,300행 | **깨짐** — 스크롤로 못 읽는다 |
| 매출 흐름 그래프 | 3개 선 | 1,300개 선 | **깨짐** — 형체가 없다 |
| 재고 반복 집계 | 90건 JSON 파싱 | **39,000건 / 화면 1회** | **깨짐** — 느려서 못 쓴다 |
| `MART_DAY_STORE` | 1,188행 | 514,800행 | 견딤 (PK 인덱스) |
| `MART_DAY_STORE_ITEM` | 108,993행 | **47,361,600행** | 상품 질의에 주의 필요 |

### 결정 1. 신호를 JSON에서 **컬럼으로** 꺼낸다

재고 부족이 며칠 반복됐는지 세려면 지금은 브리핑 JSON을 전부 열어 `cards` 를 봐야 한다.
3개 매장이면 90건이지만 1,300개면 **39,000건을 화면 열 때마다** 파싱한다.

`MART_DAY_STORE_SIGNAL` 테이블을 더해 배치가 신호를 **컬럼으로도** 남긴다.
그러면 집계가 `GROUP BY` 한 번으로 끝난다.

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `SALEDATE` | TEXT | `YYYYMMDD` (PK) |
| `DEPT_CD` | TEXT | 점포코드 (PK) |
| `STATUS` | TEXT | `STOCK`·`PEAK`·`CALM` |
| `RISK_COUNT` | INTEGER | 위험 품목 수 |

JSON은 그대로 둔다 — **진실의 원천은 여전히 브리핑**이고, 이 테이블은 그것을
집계 가능한 모양으로 옮겨 적은 것이다. 두 값이 어긋나지 않음을 테스트로 고정한다.

### 결정 2. 그룹 요약은 전 매장을 담지 않는다

`stores` 에 1,300개를 넣으면 화면이 다 그리려 든다. **상위 N개만** 담고,
나머지는 **통계**로 말한다.

```json
{
  "store_count": 1300,
  "stores": [ ... 매출 상위 20개만 ... ],
  "stores_truncated": true,
  "status_counts": {"STOCK": 412, "PEAK": 530, "CALM": 358},
  "sale_amt_quartiles": {"min": 91000, "p25": 210000, "median": 480000,
                         "p75": 1120000, "max": 3280500}
}
```

매장이 20개 이하면 `stores` 에 전부 들어가고 `stores_truncated` 는 `false` 다 —
**데모(3개)는 지금과 똑같이 동작한다.**

분위수를 넣는 이유: 1,300개에서는 평균이 거의 쓸모없다. 대형점 하나가 평균을 끌어올린다.
"내 매장이 어디쯤인가"는 중앙값과 사분위로만 답할 수 있다.

### 결정 3. 그래프의 계열 수를 고정한다

선 1,300개는 그림이 아니다. 계열은 **항상 6개 이하**로 묶는다.

| 그래프 | 매장 3개 | 매장 1,300개 |
|---|---|---|
| 매출 흐름 | 매장별 선 3개 | **전체 합계 1선 + 상위 5매장** |
| 시간대 | 매장별 막대 3계열 | **전체 합계 1계열** |

`GROUP_CHART_SERIES = 5` 로 상한을 두고, 매장이 그보다 적으면 전부 그린다.

### 결정 4. 자르기는 **DB가** 한다

1,300행을 파이썬으로 받아 `head()` 하지 않는다. `ORDER BY ... LIMIT` 으로
DB가 잘라서 준다. 상품 마트는 4,700만 행이므로 특히 그렇다.

### 결정 5. 새 자료 셋 (소유자 선택)

세 가지 모두 **`GROUP BY` 로 집계**하므로 매장 수에 따라 행이 늘지 않는다.

| 자료 | 질의 | 1,300개에서 |
|---|---|---|
| 재고 부족 반복 | `MART_DAY_STORE_SIGNAL` 최근 N일 `GROUP BY DEPT_CD` → 상위 20 | 결과 20행 고정 |
| 요일 패턴 | `MART_DAY_STORE` 최근 N주 요일별 평균 | 결과 7행 고정 |
| 월별 흐름 | `MART_DAY_STORE` 월별 **일평균** | 결과 13행 고정 |

**월별은 합계가 아니라 일평균으로 그린다** — 2월은 28일이라 합계로 보면 실제보다
낮게 보인다. 일수 차이가 만든 착시를 지표로 착각하게 두지 않는다.

요일 패턴은 **전체 합계 기준**을 기본으로 하고, 매장별은 상위 N개만 겹쳐 본다
(결정 3과 같은 이유).

### 결정 6. 데모는 그대로 보인다

모든 상한은 "매장이 그보다 적으면 전부"로 동작한다. 3개 매장 데모에서
화면이 달라지지 않으며, 1,300개에서도 같은 코드가 돈다.

## B.14 테스트 추가 (규모)

| 테스트 | 확인 |
|---|---|
| `test_signal_table_matches_briefing_json` | 신호 컬럼 = 브리핑 JSON의 카드 (어긋나지 않음) |
| `test_group_payload_caps_store_rows` | 매장이 상한을 넘으면 상위 N만 담고 표시를 남긴다 |
| `test_group_payload_keeps_all_small_store_count` | 3개면 전부 담고 `stores_truncated` 는 false |
| `test_status_counts_cover_every_store` | 상태별 개수 합 = 전체 매장 수 |
| `test_quartiles_are_ordered` | 분위수가 오름차순 |
| `test_signal_streak_is_limited_by_db` | 재고 반복 집계가 DB에서 잘려 온다 |
| `test_dow_pattern_has_seven_rows` | 요일 패턴은 매장 수와 무관하게 7행 |
| `test_monthly_trend_uses_daily_average` | 월별이 합계가 아니라 일평균 |
| `test_chart_series_never_exceeds_cap` | 그래프 계열 수 상한 |

## 명세와의 관계

- 본문 4장 DDL에 테이블 1개 추가 (기존 11개 불변).
- 본문 7장에 그룹 브리핑 생성 단계 추가 (기존 카드·문장·임계값 전부 불변).
- 본문 9장에 보기 전환과 화면 1종 추가 (기존 점포장 화면 불변).
- 본문 14장의 금지(전문용어·원인 단정·발주 수량)를 새 화면에도 그대로 적용한다.
- 불변식 1·3·7을 그대로 지킨다. 특히 **화면 무산술**은 테스트로 고정한다.
