# 30초 매장 브리핑 데모 — 런타임 의존성
# 버전은 하한만 고정한다 (ADR-0004): Streamlit Community Cloud가
# 자체 해석으로 최신 호환 버전을 고르게 둔다.

pandas>=2.2
numpy>=1.26
SQLAlchemy>=2.0
# width="stretch" 파라미터가 들어온 버전 이상이어야 한다 (ADR-0009)
streamlit>=1.49
openpyxl>=3.1

# 원격 PostgreSQL(Supabase) 연결 드라이버 (ADR-0011).
# 로컬 SQLite만 쓸 때는 없어도 되지만, 클라우드 배포에는 반드시 필요하다.
psycopg2-binary>=2.9

# 개발·검증용 (배포에도 남겨 둔다 — 클라우드에서 pytest를 돌려 볼 수 있게)
pytest>=8.0
